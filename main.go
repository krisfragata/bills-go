package main

import (
	"bufio"
	"fmt"
	"os"
)

func inputHelper(prompt string, r *bufio.Reader) (string, error) {
	fmt.Print(prompt, ":")
}

// create a func that creates an instance of bill

func createBill() bill {
	// create an instance of a reader using the Stdin
	reader := bufio.NewReader(os.Stdin)

}

// create a function that propmpts user

func main() {

}
